{
	"tasks": {
		"app:dev": "cd app && deno task dev",
		"app:lint": "cd app && deno task lint",
		"studio:dev": "cd studio && npm run dev",
		"studio:deploy": "cd studio && npm run deploy",
		"studio:codegen": "cd studio && npm run codegen"
	}
}
